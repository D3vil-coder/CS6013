[build-system]
requires = ["setuptools>=78.1.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cs6013-week05-resp20"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "safetensors==0.8.0",
    "torch==2.11.0",
    "transformers==5.16.1",
]

[tool.uv]
package = false
environments = ["sys_platform == 'linux'"]
